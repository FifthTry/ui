-- void submit(site_slug, github_org, github_repo_name, github_repo_branch):
string site_slug:
ftd.string-field $org:
ftd.string-field $repo:
ftd.string-field $branch:
js: $assets.files.assets.functions.js

callAlert("Re-Configuring github: " + org.value + "/" + repo.value + "@" + branch.value)
