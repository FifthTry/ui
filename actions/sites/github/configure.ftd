-- ftd.string-field $org: org
$value: *$ftd.empty

-- ftd.string-field $repo: repo
$value: *$ftd.empty

-- ftd.string-field $branch: branch
$value: *$ftd.empty

-- void submit(site_slug, org, repo, branch):
string site_slug:
ftd.string-field $org:
ftd.string-field $repo:
ftd.string-field $branch:
js: $assets.files.assets.functions.js

callAlert("Configuring github")
