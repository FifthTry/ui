-- ftd.string-field $domain: domain

-- void submit(site_slug, domain):
string site_slug:
ftd.string-field $domain:
js: $assets.files.assets.functions.js

callAlert("Adding domain: " + domain.value + ", for site: " + site_slug)
