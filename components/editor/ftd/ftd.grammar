@top ImportCommand { "--" "import" ":" Name }

Name {
    identifier
}

@tokens {
  identifier { $[a-zA-Z0-9_\-]+ }
  // QuotedString { '"' (!["\\] | "\\" _)* '"' }
  LineComment { ";;" ![\n]* }
  // space { $[ \t\n\r]+ }
}
